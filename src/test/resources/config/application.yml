server:
    port: 10344
    address: localhost


# security configuration (this key should be unique for your application, and kept secret)
jhipster.security.rememberme.key: afikoman

async:
    corePoolSize: 2
    maxPoolSize: 50
    queueCapacity: 10000

spring:

    data:
        mongodb:
             host: localhost
             port: 27017
             database: afikoman


    mail:
        host: smtp.gmail.com
        port: 587
        user: automail@biocatch.com
        password: [PASSWORD]
        protocol: smtp
        tls: true
        auth: true
        from: afikoman@biocatch.com

metrics:
    jmx.enabled: false

swagger:
    apiDocsLocation: http://localhost:8080
    title: jhipster API
    description: jhipster applications and beyond!
    termsOfServiceUrl: http://jhipster.github.io/
    contact:
    license: Apache 2.0
    licenseUrl: http://www.apache.org/licenses/LICENSE-2.0.html
